<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Storyteller" %></title>

    <%= stylesheet_link_tag    "application", :media => "all"  %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <nav class="top-bar" data-topbar>
      <ul class="title-area">
        <li class="name">
          <h1><a href="/">Storyteller</a></h1>
        </li>
        <li class="toggle-topbar menu-icon"><a href="#">Menu</a></li>
      </ul>

      <section class="top-bar-section">
        <!-- Right Nav Section -->
        <ul class="right">
          <% if current_user %>
          <li><a href="/signout">Welcome <%= current_user.name %>!  Click here to sign out</a></li>
          <% else %>

          <li><a href="#">Sign in</a></li>
          <li><a href="/auth/facebook">Sign in with Facebook</a></li>
          <li><a href="/">Create an account</a></li>
          <% end %>
        </ul>

        <!-- Left Nav Section -->
        <ul class="left">
          <li><a href='/writers'>View All Writers</a></li>
        </ul>
      </section>

    </nav>
<div class-"row">
</br></div>
    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>